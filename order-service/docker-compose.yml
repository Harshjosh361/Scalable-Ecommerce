services:
  mongo:
    container_name: mongo
    image: mongo:latest
    restart: always
    ports:
      - "27020:27017"
      
  goapp:
    build: .
    container_name: goapp
    ports:
      - "8083:8083"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/
