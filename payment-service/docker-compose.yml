services:
  mongo:
    container_name: mongo
    image: mongo:latest
    restart: always
    ports:
      - "27019:27017"
      
  goapp:
    build: .
    container_name: goapp
    ports:
      - "8082:8082"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/
